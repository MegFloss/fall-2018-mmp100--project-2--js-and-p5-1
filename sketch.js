const IMAGE_WIDTH = 800;
const IMAGE_HEIGHT = 600;

function setup() {
   createCanvas(IMAGE_WIDTH,
      IMAGE_HEIGHT);
      background (200);
   noLoop();
  }

function draw() {
  stroke(198, 157, 101);
  line(182, 138, 216, 170, 244, 190);

}
